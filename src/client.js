import React from 'react'
import ReactDOM from 'react-dom'
import App from '../src/app/App.jsx'

// Renders the react App container onto the page
ReactDOM.render(<App />, document.getElementById('root'))
